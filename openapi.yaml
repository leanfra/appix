# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Envs API
    version: 0.0.1
paths:
    /api/v1/envs/create:
        post:
            tags:
                - Envs
            operationId: Envs_CreateEnvs
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateEnvsRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/CreateEnvsReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/v1/envs/delete:
        post:
            tags:
                - Envs
            operationId: Envs_DeleteEnvs
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/DeleteEnvsRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DeleteEnvsReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/v1/envs/list:
        post:
            tags:
                - Envs
            operationId: Envs_ListEnvs
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/ListEnvsRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListEnvsReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/v1/envs/update:
        post:
            tags:
                - Envs
            operationId: Envs_UpdateEnvs
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateEnvsRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/UpdateEnvsReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/v1/envs/{id}:
        get:
            tags:
                - Envs
            operationId: Envs_GetEnvs
            parameters:
                - name: id
                  in: path
                  required: true
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetEnvsReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        CreateEnvsReply:
            type: object
            properties:
                message:
                    type: string
                code:
                    type: string
                action:
                    type: string
        CreateEnvsRequest:
            type: object
            properties:
                envs:
                    type: array
                    items:
                        $ref: '#/components/schemas/Env'
        DeleteEnvsReply:
            type: object
            properties:
                message:
                    type: string
                code:
                    type: string
                action:
                    type: string
        DeleteEnvsRequest:
            type: object
            properties:
                ids:
                    type: array
                    items:
                        type: string
        Env:
            type: object
            properties:
                id:
                    type: string
                name:
                    type: string
                description:
                    type: string
            description: gratos::model
        EnvFilter:
            type: object
            properties:
                name:
                    type: string
            description: gratos::model
        GetEnvsReply:
            type: object
            properties:
                message:
                    type: string
                code:
                    type: string
                action:
                    type: string
                env:
                    $ref: '#/components/schemas/Env'
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        ListEnvsFilter:
            type: object
            properties:
                page:
                    type: string
                pageSize:
                    type: string
                filters:
                    type: array
                    items:
                        $ref: '#/components/schemas/EnvFilter'
            description: gratos::model
        ListEnvsReply:
            type: object
            properties:
                message:
                    type: string
                code:
                    type: string
                action:
                    type: string
                envs:
                    type: array
                    items:
                        $ref: '#/components/schemas/Env'
        ListEnvsRequest:
            type: object
            properties:
                filter:
                    $ref: '#/components/schemas/ListEnvsFilter'
            description: ListEnvsRequst has no fields because envs count is not very big
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
        UpdateEnvsReply:
            type: object
            properties:
                message:
                    type: string
                code:
                    type: string
                action:
                    type: string
        UpdateEnvsRequest:
            type: object
            properties:
                envs:
                    type: array
                    items:
                        $ref: '#/components/schemas/Env'
tags:
    - name: Envs
