# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Clusters API
    version: 0.0.1
paths:
    /api/v1/clusters/create:
        post:
            tags:
                - Clusters
            operationId: Clusters_CreateClusters
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateClustersRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/CreateClustersReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/v1/clusters/delete:
        post:
            tags:
                - Clusters
            operationId: Clusters_DeleteClusters
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/DeleteClustersRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DeleteClustersReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/v1/clusters/list:
        post:
            tags:
                - Clusters
            operationId: Clusters_ListClusters
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/ListClustersRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListClustersReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/v1/clusters/update:
        post:
            tags:
                - Clusters
            operationId: Clusters_UpdateClusters
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateClustersRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/UpdateClustersReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/v1/clusters/{id}:
        get:
            tags:
                - Clusters
            operationId: Clusters_GetClusters
            parameters:
                - name: id
                  in: path
                  required: true
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetClustersReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        Cluster:
            type: object
            properties:
                id:
                    type: string
                name:
                    type: string
                description:
                    type: string
            description: gratos::model
        ClusterFilter:
            type: object
            properties:
                name:
                    type: string
            description: gratos::model
        CreateClustersReply:
            type: object
            properties:
                message:
                    type: string
                code:
                    type: string
                action:
                    type: string
        CreateClustersRequest:
            type: object
            properties:
                clusters:
                    type: array
                    items:
                        $ref: '#/components/schemas/Cluster'
        DeleteClustersReply:
            type: object
            properties:
                message:
                    type: string
                code:
                    type: string
                action:
                    type: string
        DeleteClustersRequest:
            type: object
            properties:
                ids:
                    type: array
                    items:
                        type: string
        GetClustersReply:
            type: object
            properties:
                message:
                    type: string
                code:
                    type: string
                action:
                    type: string
                cluster:
                    $ref: '#/components/schemas/Cluster'
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        ListClustersFilter:
            type: object
            properties:
                page:
                    type: string
                pageSize:
                    type: string
                filters:
                    type: array
                    items:
                        $ref: '#/components/schemas/ClusterFilter'
            description: gratos::model
        ListClustersReply:
            type: object
            properties:
                message:
                    type: string
                code:
                    type: string
                action:
                    type: string
                clusters:
                    type: array
                    items:
                        $ref: '#/components/schemas/Cluster'
        ListClustersRequest:
            type: object
            properties:
                filter:
                    $ref: '#/components/schemas/ListClustersFilter'
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
        UpdateClustersReply:
            type: object
            properties:
                message:
                    type: string
                code:
                    type: string
                action:
                    type: string
        UpdateClustersRequest:
            type: object
            properties:
                clusters:
                    type: array
                    items:
                        $ref: '#/components/schemas/Cluster'
tags:
    - name: Clusters
