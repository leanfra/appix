# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Teams API
    version: 0.0.1
paths:
    /api/v1/teams/create:
        post:
            tags:
                - Teams
            operationId: Teams_CreateTeams
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateTeamsRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/CreateTeamsReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/v1/teams/delete:
        post:
            tags:
                - Teams
            operationId: Teams_DeleteTeams
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/DeleteTeamsRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DeleteTeamsReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/v1/teams/list:
        post:
            tags:
                - Teams
            operationId: Teams_ListTeams
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/ListTeamsRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListTeamsReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/v1/teams/update:
        post:
            tags:
                - Teams
            operationId: Teams_UpdateTeams
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateTeamsRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/UpdateTeamsReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/v1/teams/{id}:
        get:
            tags:
                - Teams
            operationId: Teams_GetTeams
            parameters:
                - name: id
                  in: path
                  required: true
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetTeamsReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        CreateTeamsReply:
            type: object
            properties:
                message:
                    type: string
                code:
                    type: string
                action:
                    type: string
        CreateTeamsRequest:
            type: object
            properties:
                teams:
                    type: array
                    items:
                        $ref: '#/components/schemas/Team'
        DeleteTeamsReply:
            type: object
            properties:
                message:
                    type: string
                code:
                    type: string
                action:
                    type: string
        DeleteTeamsRequest:
            type: object
            properties:
                ids:
                    type: array
                    items:
                        type: string
        GetTeamsReply:
            type: object
            properties:
                message:
                    type: string
                code:
                    type: string
                action:
                    type: string
                team:
                    $ref: '#/components/schemas/Team'
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        ListTeamsFilter:
            type: object
            properties:
                page:
                    type: string
                pageSize:
                    type: string
                filters:
                    type: array
                    items:
                        $ref: '#/components/schemas/TeamFilter'
            description: gratos::model
        ListTeamsReply:
            type: object
            properties:
                message:
                    type: string
                code:
                    type: string
                action:
                    type: string
                teams:
                    type: array
                    items:
                        $ref: '#/components/schemas/Team'
        ListTeamsRequest:
            type: object
            properties:
                filter:
                    $ref: '#/components/schemas/ListTeamsFilter'
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
        Team:
            type: object
            properties:
                id:
                    type: string
                name:
                    type: string
                code:
                    type: string
                description:
                    type: string
                leader:
                    type: string
            description: gratos::model
        TeamFilter:
            type: object
            properties:
                name:
                    type: string
                code:
                    type: string
                leader:
                    type: string
            description: gratos::model
        UpdateTeamsReply:
            type: object
            properties:
                message:
                    type: string
                code:
                    type: string
                action:
                    type: string
        UpdateTeamsRequest:
            type: object
            properties:
                teams:
                    type: array
                    items:
                        $ref: '#/components/schemas/Team'
tags:
    - name: Teams
