# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Datacenters API
    version: 0.0.1
paths:
    /api/v1/datacenters/create:
        post:
            tags:
                - Datacenters
            operationId: Datacenters_CreateDatacenters
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateDatacentersRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/CreateDatacentersReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/v1/datacenters/delete:
        post:
            tags:
                - Datacenters
            operationId: Datacenters_DeleteDatacenters
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/DeleteDatacentersRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DeleteDatacentersReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/v1/datacenters/list:
        post:
            tags:
                - Datacenters
            operationId: Datacenters_ListDatacenters
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/ListDatacentersRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListDatacentersReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/v1/datacenters/update:
        post:
            tags:
                - Datacenters
            operationId: Datacenters_UpdateDatacenters
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateDatacentersRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/UpdateDatacentersReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/v1/datacenters/{id}:
        get:
            tags:
                - Datacenters
            operationId: Datacenters_GetDatacenters
            parameters:
                - name: id
                  in: path
                  required: true
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetDatacentersReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        CreateDatacentersReply:
            type: object
            properties:
                message:
                    type: string
                code:
                    type: string
                action:
                    type: string
        CreateDatacentersRequest:
            type: object
            properties:
                datacenters:
                    type: array
                    items:
                        $ref: '#/components/schemas/Datacenter'
        Datacenter:
            type: object
            properties:
                id:
                    type: string
                name:
                    type: string
                description:
                    type: string
            description: gratos::model
        DatacenterFilter:
            type: object
            properties:
                name:
                    type: string
            description: gratos::model
        DeleteDatacentersReply:
            type: object
            properties:
                message:
                    type: string
                code:
                    type: string
                action:
                    type: string
        DeleteDatacentersRequest:
            type: object
            properties:
                ids:
                    type: array
                    items:
                        type: string
        GetDatacentersReply:
            type: object
            properties:
                message:
                    type: string
                code:
                    type: string
                action:
                    type: string
                datacenter:
                    $ref: '#/components/schemas/Datacenter'
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        ListDatacentersFilter:
            type: object
            properties:
                page:
                    type: string
                pageSize:
                    type: string
                filters:
                    type: array
                    items:
                        $ref: '#/components/schemas/DatacenterFilter'
            description: gratos::model
        ListDatacentersReply:
            type: object
            properties:
                message:
                    type: string
                code:
                    type: string
                action:
                    type: string
                datacenters:
                    type: array
                    items:
                        $ref: '#/components/schemas/Datacenter'
        ListDatacentersRequest:
            type: object
            properties:
                filter:
                    $ref: '#/components/schemas/ListDatacentersFilter'
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
        UpdateDatacentersReply:
            type: object
            properties:
                message:
                    type: string
                code:
                    type: string
                action:
                    type: string
        UpdateDatacentersRequest:
            type: object
            properties:
                datacenters:
                    type: array
                    items:
                        $ref: '#/components/schemas/Datacenter'
tags:
    - name: Datacenters
